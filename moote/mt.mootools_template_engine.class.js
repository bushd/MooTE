var MooTE = new CLass({

	Implements: [Options],

	options: {
		template: '',
		templateURL: undefined,
		helper: {},
		substitude
	},

	initialize: function(options) {
		this.setOptions(options);
	}
});